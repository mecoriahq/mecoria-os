# Internal Visual Render QA Agent

Performs technical validation on internally rendered PNG visual assets.

This agent checks file existence, readability, PNG format, resolution, file size, and Pillow compatibility.
