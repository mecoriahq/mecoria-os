# Visual Asset Production Manifest Agent

Creates production folders and instruction files for every reusable visual asset in the Visual Asset Plan.

This agent does not create media assets. It prepares the production workspace.
