# Internal Visual Production Agent

Creates first internal visual assets for Hiddenova.

The MVP creates reusable SVG graphics for internal asset types such as map animations, diagrams, and text overlays.
