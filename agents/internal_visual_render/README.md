# Internal Visual Render Agent

Renders internally produced visual assets into PNG files that can be used by video assembly.

The MVP uses Pillow to create PNG graphics directly from internal asset definitions.
