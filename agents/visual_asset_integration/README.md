# Visual Asset Integration Agent

Combines the reusable visual asset plan, acquisition routing, rendered internal PNG assets, and render QA output into one scene-to-visual integration package.

This package is used by Video Assembly v1 to understand which scenes already have usable internal visuals and which scenes still need stock footage or manual capture.
